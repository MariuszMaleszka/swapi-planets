export type PlanetType = {
  name: string;
  population: string;
  populationNumber: number;
  rotation_period: string;
  climate: string;
  gravity: string;
  gravityNumber: number;
  created: string;
  url: string;
};
